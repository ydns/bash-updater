Our periodical domain check has detected that a DNS query for domain "{{ domain.name }}" has failed.

    {{ message }}


==== DOMAIN DETAILS ====

Domain name   : {{ domain.name }} (ID#{{ domain.id }})
Creation Date : {{ domain.date_created }}
Owner         : {{ domain.owner.email|default:'(user has gone)' }}

If the domain remains in this state for more than 7 days, the domain is being deleted automatically.
The owner and all users using that domain are notified via Email if that happens.